[pytest]
testpaths = tests
# The app has been migrated off the legacy Query.get() API, so surface any
# newly-introduced legacy usage as an error rather than silently warning.
filterwarnings =
    error::sqlalchemy.exc.LegacyAPIWarning
